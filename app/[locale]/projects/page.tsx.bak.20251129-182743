// app/[locale]/projects/page.tsx
import Topbar from "@/components/Topbar";
import { getDictionary } from "@/lib/i18n/get-dictionary";
import type { Locale } from "@/lib/i18n/settings";

type Props = {
  params: { locale: Locale };
};

export default async function ProjectsPage({ params }: Props) {
  const { locale } = params;
  const dict = await getDictionary(locale);
  const t = dict?.projects ?? {};

  return (
    <>
      <Topbar locale={locale} />

      <main className="na-page">
        {/* Page hero */}
        <section className="na-page-hero">
          <h1 className="na-page-title">
            {t.title ?? "Our Projects"}
          </h1>
          <p className="na-page-lead">
            {t.motto ??
              "Each project is a promise to the future of Haiti. This blog highlights the thinking, collaboration, and technical work that support these initiatives."}
          </p>
        </section>

        {/* Project cards */}
        <section className="na-card-grid">
          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.cleanWaterTitle ?? "Clean Water"}
            </h2>
            <p className="na-card-body">
              {t.cleanWaterBody ?? "Providing safe drinking water to communities."}
            </p>
          </article>

          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.educationTitle ?? "Education"}
            </h2>
            <p className="na-card-body">
              {t.educationBody ?? "Schools for the future generation."}
            </p>
          </article>

          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.healthcareTitle ?? "Healthcare"}
            </h2>
            <p className="na-card-body">
              {t.healthcareBody ?? "Access to medical centers and basic care."}
            </p>
          </article>

          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.infrastructureTitle ?? "Infrastructure"}
            </h2>
            <p className="na-card-body">
              {t.infrastructureBody ??
                "Rebuilding roads, bridges, and public spaces."}
            </p>
          </article>

          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.agricultureTitle ?? "Agriculture"}
            </h2>
            <p className="na-card-body">
              {t.agricultureBody ??
                "Supporting local farmers and food security."}
            </p>
          </article>

          <article className="na-card na-card--project">
            <h2 className="na-card-title">
              {t.technologyTitle ?? "Technology"}
            </h2>
            <p className="na-card-body">
              {t.technologyBody ??
                "Digital inclusion and skills programs."}
            </p>
          </article>
        </section>

        <p style={{ marginTop: "2rem", fontSize: "0.8rem" }}>
          Locale: {locale.toUpperCase()}
        </p>
      </main>
    </>
  );
}
