/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    // ✅ Make this list match PUBLIC_LOCALES in middleware.ts
    locales: ["en", "fr", "ht", "es"],
    defaultLocale: "en",
  },

  // 🚫 No experimental.appDir here
  // 🚫 No redirects for now – we’ll let middleware or explicit URLs handle locale
};

export default nextConfig;
