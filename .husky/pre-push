#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

# 🔑 One unified command: validate + lint + test
npm run ci-check

echo "🎉 Pre-push checks passed! Proceeding with push..."
